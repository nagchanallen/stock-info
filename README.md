# stock-info

## Backend

### Setup

In `/backend` directory:

Create environment variable file

```bash
cp .env.example .env
```

### Run Server

```bash
# Build image
docker-compose build

# Run server
docker-compose up
```
